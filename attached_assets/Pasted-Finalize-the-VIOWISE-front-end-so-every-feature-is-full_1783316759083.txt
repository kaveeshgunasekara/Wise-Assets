Finalize the VIOWISE front end so every feature is fully functional, but WITHOUT hardcoded sample users. Real users (5 mentors, 5 learners) will be added later via the database. Keep everything routed through the service layer so the database swap is a one-file change. Edit surgically; don't regenerate working files.

1. REMOVE hardcoded sample data


Empty out the hardcoded arrays of sample mentors/learners and sample posts in seed.ts (and any component that still has its own hardcoded lists — AIMatching, Wall Requests, etc.).
Keep the TYPE definitions and the seed.ts FILE structure — just make the data arrays empty (or a single placeholder you can delete). The app must run with zero or few users without crashing.


2. All data flows through the service layer (src/services/api.ts)

Every function must work against whatever data exists (empty, few, or many) — never assume a fixed set:


getUsers / getUserById, getPosts, createPost, editPost
getMatches(userId) — scores whatever opposite-role users exist
getMatchReason(a,b) — templated now, TODO(backend) for Claude
requestCall, getRequests, respondRequest
getStorySummary — placeholder now, TODO(backend) for Claude
logInteraction — console.log now, TODO(backend)
These stay as the single data access point. Later they'll call Supabase; components never change.


3. Handle EMPTY STATES gracefully (important with no seed data)

Every list must render cleanly when empty, with a friendly one-line message — never a blank or broken screen:


Wisdom Wall (For you / All wisdom): "No wisdom shared yet — be the first to post."
My posts: "Your posts will appear here."
Requests: "No call requests yet."
AI Matching: "No matches yet — complete your profile to find people."
This matters because the app will run empty until real users are added.


4. Confirm every feature is FUNCTIONAL (against real, not sample, data)


Onboarding: home → role → sign-up → topics + languages + ID verify → verified → AUTO sign-in (log the user in directly, land on their page, do NOT display a password).
A newly signed-up user is a REAL user in app state (name, role, topics, languages) and can immediately use everything.
Posting: create + edit (NO delete). Mentors post "Wisdom", learners post "Reflection". Direct posts publish live. Call-summary posts need partner approval.
Wisdom Wall: For you (ranked by the viewer's topics, rank-not-hide) / All wisdom (unranked) / My posts / Requests. Both roles have all four tabs.
Request a call: on any post and any match, BOTH directions (learner→mentor "seek", mentor→learner "offer") → writes a real request → appears in the recipient's Requests tab → Accept (→consent→call) / Decline.
AI Matching: getMatches ranks opposite-role users by shared topics (+languages), shows computed % with a driver ("92% · 3 shared topics") and a "see why" reason.
Profile: edit topics/languages/availability/bio/name; persists in session.
Pre-call consent: 3 toggles with VISIBLE On/Off text; they actually affect the call (subtitles hide if off; story step skipped if capture off).
Video call (MOCKED stream): portrait + self-view, rotating subtitles (respect the toggle), prompt card, Mute/Camera, End call, two-step Report, AND the accessibility control present here too.
Story capture: summary + Edit / Keep private / Share; Share → partner approval → appears with New badge.
Accessibility control (text size + high contrast) on ALL 12 pages including the call. Nothing under 16px. Background line-art motif on all light pages.


5. Keep it backend-ready


All types in types.ts. TODO(backend) markers where Supabase + Claude plug in.
Because there's no sample data, this shell is exactly what will connect to the database: adding a user in the DB later makes them appear everywhere automatically, since everything reads through the service layer.


ACCEPTANCE


App runs with NO sample users without crashing; every empty list shows its friendly message.
Sign up a brand-new user live → they exist in app state → can post, get matched (once others exist), request calls, edit profile — all working.
Create a second user (sign up again in another session/role) → the two can see each other's posts, request calls, and requests land correctly. This proves the whole loop works on REAL (user-created) data, not samples.
Every feature from section 4 works against user-created data.
No hardcoded people anywhere; all data via the service layer; TODO markers present for the backend.


Do NOT add sample mentors/learners or sample posts — the app should start empty and fill with real users I add later via the database.