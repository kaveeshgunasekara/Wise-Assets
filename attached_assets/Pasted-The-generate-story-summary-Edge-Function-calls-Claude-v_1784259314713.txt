The generate-story-summary Edge Function calls Claude via AWS Bedrock with SigV4 signing. Help me debug why it's not returning real Claude output. Do this step by step:

1. First, check the Edge Function logs (or tell me exactly where to find them in the Supabase dashboard) and look for the most recent line starting with "[story-summary] Bedrock HTTP" or "[story-summary] Bedrock call threw" or "[story-summary] Bedrock returned empty". Report the EXACT error status code and full response body — do not paraphrase.

2. Based on the exact error, diagnose which of these it is:
   - 403 SignatureDoesNotMatch → the SigV4 signing has a mismatch. Check: (a) the canonicalUri used for signing is byte-identical to the path in the fetch URL, (b) the payload hash is SHA256 of the exact body string sent, (c) x-amz-date in the header exactly matches the one in the string-to-sign, (d) the colon in the model ID is encoded consistently (%3A) in BOTH the signed canonicalUri and the actual request URL.
   - 403 AccessDeniedException / "You don't have access to the model" → the AWS credentials don't have Bedrock permission for this model, OR the model needs the "us." inference-profile prefix. Try changing BEDROCK_MODEL_ID to "us.anthropic.claude-haiku-4-5-20251001-v1:0".
   - 400 ValidationException about inference profiles → same fix: add the "us." prefix to the model ID.
   - 400 ValidationException about the body → check the request body format matches Bedrock's expected schema for Claude (anthropic_version: "bedrock-2023-05-31", max_tokens, messages).
   - 401 / UnrecognizedClient → the AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY secret is wrong or not set. Confirm all three secrets (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION) are set in Supabase.
   - Region mismatch → confirm AWS_REGION is us-west-2 and the endpoint host uses the same region.

3. Add a temporary debug log RIGHT BEFORE the fetch call that prints (to the Edge Function logs): the full endpoint URL, the amzDate, the canonicalRequest string, the stringToSign, and the signedHeaders — so I can see exactly what's being signed. Do NOT log the secret access key or the signature itself.

4. Tell me the single most likely cause based on the actual logged error, and give me the exact one-line fix.

Report the exact error from the logs first, THEN the diagnosis. Don't guess before reading the actual error.